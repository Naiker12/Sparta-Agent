╔══════════════════════════════════════════════════════════════════════════════╗
║                     SPARTA AGENT — DOCUMENTACIÓN DEL PROYECTO              ║
║                     Versión documentada: 0.8.0                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

Nota: la versión se lee desde package.json vía IPC (app:getVersion) y se muestra
en StatusBar dinámicamente. package.json debe actualizarse manualmente antes de
cada release.

================================================================================
 1. ¿QUÉ ES SPARTA AGENT?
================================================================================

Sparta Agent es una aplicación de escritorio para orquestar agentes de IA,
construida con React + TypeScript + Vite + Electron. Proporciona una interfaz
unificada de chat, gestión de sesiones, skills reutilizables, servidores MCP,
canales de comunicación, memoria persistente con grafo 3D, proyectos y
proveedores de modelos de lenguaje.

Soporta el Protocolo de Contexto de Modelo (MCP), 13 temas visuales
(8 oscuros + 5 claros), internacionalización español/inglés, iconos de marca
con detección de tema, y un bus de eventos tipado en memoria. Es un
"IDE de agentes de IA" local-first en desarrollo activo.

Estado actual (v0.8.0):
  • UI/UX muy pulida y funcional (layout, sidebar, chat, settings, views)
  • Gestión de sesiones, skills, MCP, canales, memoria y proyectos operativa
  • Sistema de proveedores con test connection real y fetch de modelos
  • Grafo 3D de memoria con Three.js interactivo + extracción automática
  • Capa de IA funcional: transports (Anthropic, OpenAI, Ollama), factory, gateway
  • Chat real con streaming token-a-token vía IPC (main process)
  • ThinkingBlock / ThinkingPill / PipelineTrace con datos reales del proveedor
  • Abortar generación (AbortSignal + botón Stop)
  • Memoria como grafo de conocimiento: extracción de entidades/hechos/relaciones
  • Interfaces separadas en src/interfaces/ para mejor práctica
  • Capa de servicios (src/services/) con arquitectura limpia
  • 3 stores que iniciaban con datos demo ahora inician vacíos
  • Editor, Terminal y Agents Panel son placeholders visuales

================================================================================
 2. STACK TECNOLÓGICO
================================================================================

  Capa              Tecnología
  ─────────────────────────────────────────────────────────────
  UI Framework      React 18 + TypeScript
  Bundler           Vite 5
  Desktop Shell     Electron 30
  State Management  Zustand 5 (persist middleware en 9 stores)
  UI Library        shadcn/ui (@base-ui/react, class-variance-authority)
  Estilos           Tailwind CSS v4
  Fuentes           Inter (UI), Space Grotesk (Display), Geist Variable (Mono)
  Iconos            Lucide React + SVGs propios en public/icons/brands/
  Animaciones       Framer Motion 12
  Editor Código     Monaco Editor (@monaco-editor/react) — instalado, no integrado
  Terminal          xterm.js (@xterm/xterm + @xterm/addon-fit) — instalado, no integrado
  Async/Datos       TanStack React Query 5 — instalado, uso mínimo
  Grafo 3D          three.js 0.184
  Package Manager   pnpm
  Builder           electron-builder 24
  CLI UI            shadcn ^4.11.0

================================================================================
 3. ESTRUCTURA DEL PROYECTO (Árbol Completo)
================================================================================

D:\sparta-agent\
│
├── .eslintrc.cjs                  # Config ESLint
├── .gitignore                     # Reglas git ignore
├── components.json                # Registro shadcn/ui
├── electron-builder.json5         # Config empaquetado Electron
├── index.html                     # Entry point HTML de Vite
├── package.json                   # Manifiesto del proyecto (version 0.0.0)
├── pnpm-lock.yaml                 # Lock file
├── pnpm-workspace.yaml            # Config workspace pnpm
├── tsconfig.json                  # TypeScript config (src + electron)
├── tsconfig.node.json             # TypeScript config (vite.config.ts)
├── vite.config.ts                 # Config bundler Vite
├── README.md                      # Readme (template Vite default)
├── DOCUMENTACION_PROYECTO.txt     # Este documento
│
├── docs\                          # Documentación técnica adicional
│   └── barra-lateral-flujo.txt    # Especificación del sidebar
│
├── electron\                      # PROCESO PRINCIPAL DE ELECTRON
│   ├── electron-env.d.ts          # Tipos para APIs de Electron
│   ├── main.ts                    # Ventana principal, titleBarOverlay, IPC handlers
│   ├── preload.ts                 # Preload: contextBridge (electronAPI, electron, sparta)
│   └── ipc\                       # IPC handlers por dominio
│       ├── chat.ipc.ts            # chat:send (streaming SSE multi-proveedor), chat:abort (AbortSignal)
│       └── memory.ipc.ts          # memory:list/add/update/delete + relations
│
├── public\                        # ASSETS ESTÁTICOS
│   ├── electron-vite.animate.svg
│   ├── escritorio.png
│   ├── favicon-96x96.png
│   ├── favicon.ico
│   ├── favicon.svg
│   ├── site.webmanifest
│   ├── sparta-escritorio.png
│   ├── sparta-icon.png
│   ├── web-app-manifest-192x192.png
│   ├── web-app-manifest-512x512.png
│   ├── blanco\                    # Iconos variante blanca
│   ├── negro\                     # Iconos variante negra
│   ├── icons\brands\              # 26+ SVGs de marcas (AI, integrations, tools)
│   └── skills\                    # 12 skills descargables .skill.json + manifest.json
│
├── src\                           # CÓDIGO FUENTE DE LA APLICACIÓN
│   │
│   ├── main.tsx                   # Entry point React, renderiza <AppShell>
│   ├── App.tsx                    # Placeholders: EditorPanel, TerminalPanel, AgentsPanel
│   ├── vite-env.d.ts              # Referencia Vite client types
│   ├── index.css                  # Tailwind v4 import + @theme inline + animaciones custom
│   │
│   ├── assets\                    # Assets empaquetados
│   │   ├── react.svg              # Logo React por defecto
│   │   └── sparta-icon.png        # Icono Sparta para chat
│   │
│   ├── i18n\                      # SISTEMA DE INTERNACIONALIZACIÓN
│   │   ├── index.ts               # Hook useTranslation() + getNestedValue
│   │   ├── es.ts                  # Diccionario español
│   │   └── en.ts                  # Diccionario inglés
│   │
│   ├── interfaces\                # INTERFACES PURAS (contratos)
│   │   ├── index.ts               # Barrel export
│   │   ├── chat.interface.ts      # Message, Session, ToolCall, ChatStreamChunk, ChatRequest
│   │   ├── provider.interface.ts  # Provider, AIProvider, ModelInfo, ProviderVendor
│   │   ├── mcp.interface.ts       # MCPServer, MCPTool, MCPServerConfig
│   │   ├── memory.interface.ts    # MemoryEntry, MemoryRelation, MemoryGraph, ExtractedMemory
│   │   ├── agent.interface.ts     # Agent, Task, TaskStep
│   │   ├── skill.interface.ts     # Skill, DownloadableSkill
│   │   ├── channel.interface.ts   # Channel, ChannelMessage, IntegrationConfig
│   │   ├── project.interface.ts   # Project
│   │   └── settings.interface.ts  # SettingsTab (unificada), SessionMode, Language
│   │
│   ├── services\                  # CAPA DE SERVICIOS (casos de uso, sin React)
│   │   ├── index.ts               # Barrel export
│   │   ├── ai\                    # Capa de IA
│   │   │   ├── index.ts           # createProvider()
│   │   │   ├── types.ts           # AIProvider interface
│   │   │   ├── gateway.ts         # AIGateway (punto único para main process)
│   │   │   └── transports\        # Transportes por proveedor
│   │   │       ├── index.ts
│   │   │       ├── base.ts
│   │   │       ├── anthropic.transport.ts
│   │   │       ├── openai.transport.ts  # OpenAI/Groq/Mistral/DeepSeek/etc
│   │   │       └── ollama.transport.ts
│   │   ├── chat\                  # ChatService
│   │   │   └── index.ts
│   │   ├── mcp\                   # MCPClient
│   │   │   └── index.ts
│   │   └── memory\                # Memoria como grafo
│   │       ├── extractor.ts       # Extracción LLM de entidades/hechos/relaciones
│   │       ├── graph-writer.ts    # Dedup + trust scoring + escritura
│   │       └── graph-layout.ts    # Layout Fibonacci incremental
│   │
│   ├── lib\                       # UTILIDADES
│   │   ├── utils.ts               # cn() (clsx + tailwind-merge)
│   │   ├── brand-icons.ts         # Mapa vendor → rutas SVG (24+ marcas) + MONO_BRANDS
│   │   ├── fetch-models.ts        # fetchModelsByVendor() → APIs reales (próximo a migrar a services/ai)
│   │   ├── graph-layout.ts        # Re-exporta desde services/memory/graph-layout
│   │   └── graph-colors.ts        # Extrae colores CSS vars a hex para Three.js
│   │
│   ├── types\                     # SISTEMA DE TIPOS (re-exporta desde interfaces)
│   │   ├── index.ts               # Re-exporta todos los tipos + interfaces
│   │   ├── agent.ts               # Re-export desde interfaces
│   │   ├── chat.ts                # Re-export desde interfaces
│   │   ├── events.ts              # SpartaEvent + 36 interfaces de eventos
│   │   ├── mcp.ts                 # Re-export desde interfaces
│   │   ├── project.ts             # Re-export desde interfaces
│   │   ├── skill.ts               # Re-export desde interfaces
│   │   ├── channel.ts             # Re-export desde interfaces
│   │   ├── memory.ts              # Re-export desde interfaces
│   │   └── provider.ts            # Re-export desde interfaces
│   │
│   ├── stores\                    # ESTADO GLOBAL — Zustand (12 stores)
│   │   ├── agent.store.ts         # Agentes (vacío por defecto) + tareas (en memoria)
│   │   ├── chat.store.ts          # Sesiones + mensajes + pin/archive/rename (persist)
│   │   ├── channel.store.ts       # Canales + integraciones (persist)
│   │   ├── event-bus.store.ts     # Bus de eventos pub/sub (en memoria)
│   │   ├── mcp.store.ts           # Servidores MCP (en memoria)
│   │   ├── memory.store.ts        # Entradas de memoria (persist)
│   │   ├── project.store.ts       # Proyectos (persist)
│   │   ├── provider.store.ts      # Proveedores de modelo (persist)
│   │   ├── settings.store.ts      # UI settings + toggles (persist)
│   │   ├── skill.store.ts         # Skills + activación (persist)
│   │   ├── theme.store.ts         # Tema actual + 13 temas (persist)
│   │   └── ui.store.ts            # Layout, mainView, sidebar (persist parcial)
│   │
│   ├── hooks\                     # REACT HOOKS (11 hooks)
│   │   ├── useAgent.ts
│   │   ├── useChatSession.ts
│   │   ├── useChannels.ts
│   │   ├── useEventBus.ts
│   │   ├── useMCP.ts
│   │   ├── useMemory.ts
│   │   ├── use-mobile.ts          # Hook de shadcn/ui para responsive
│   │   ├── useProject.ts
│   │   ├── useSettings.ts
│   │   ├── useSkills.ts
│   │   └── useTheme.ts
│   │
│   ├── styles\                    # ESTILOS
│   │   ├── globals.css            # Design tokens, fuentes, 13 temas, keyframes, sidebar
│   │   └── themes\                # Archivos CSS por tema
│   │       ├── amber.css
│   │       ├── catppuccin.css
│   │       ├── dracula.css
│   │       ├── emerald.css
│   │       ├── light.css
│   │       ├── linen.css
│   │       ├── midnight.css       # default
│   │       ├── mist.css
│   │       ├── nord.css
│   │       ├── obsidian.css
│   │       ├── rose.css
│   │       └── sage.css
│   │       └── sand.css
│   │
│   ├── electron\                  # Electron IPC (reservado)
│   │   └── ipc\                   # (vacío — handlers viven en electron/ipc/)
│   │
│   ├── scripts\                   # SCRIPTS DE DESARROLLO
│   │   └── seed-dev-data.ts       # Seed data para desarrollo (npm run seed)
│   │
│   └── components\                # COMPONENTES REACT (~84 archivos)
│       │
│       ├── layout\                # LAYOUT (4 componentes)
│       │   ├── AppShell.tsx       # Shell raíz con SidebarProvider + panel activo
│       │   ├── TitleBar.tsx       # Barra superior custom (drag, tabs, settings)
│       │   ├── AppMenu.tsx        # Menú visual File/Edit/View/Window/Help
│       │   └── StatusBar.tsx      # Barra inferior: gateway, agents, cron, modelo, tokens, versión
│       │
│       ├── sidebar\               # SIDEBAR (8 componentes)
│       │   ├── AppSidebar.tsx     # Sidebar principal completo
│       │   ├── SidebarResizeHandle.tsx  # Redimensionamiento 220-480px
│       │   ├── SessionList.tsx    # Lista simple de sesiones
│       │   ├── SessionItem.tsx    # Item de sesión con menú (pin/rename/share/delete)
│       │   ├── ProjectSwitcher.tsx# Selector de proyecto activo
│       │   ├── ChannelItem.tsx    # Item de canal (visual legacy)
│       │   ├── McpServerItem.tsx  # Item de MCP (visual legacy)
│       │   └── SkillItem.tsx      # Item de skill (visual legacy)
│       │
│       ├── chat\                  # CHAT (8 componentes + reasoning)
│       │   ├── ChatArea.tsx       # Panel de chat: HeroScreen o MessageList + ChatInput
│       │   ├── HeroScreen.tsx     # Pantalla de inicio con branding + acciones rápidas
│       │   ├── MessageBubble.tsx  # Burbuja de mensaje individual
│       │   ├── MessageList.tsx    # Lista scrollable de mensajes
│       │   ├── MessageActionsMenu.tsx   # Menú contextual del mensaje
│       │   ├── MessageActionsDialog.tsx # Diálogos de confirmación
│       │   ├── SpartaIcon.tsx     # SVG del icono de Sparta
│       │   └── reasoning\         # Razonamiento
│       │       ├── index.ts
│       │       ├── ThinkingBlock.tsx
│       │       ├── ThinkingPill.tsx
│       │       └── PipelineTrace.tsx
│       │
│       ├── input\                 # INPUT (5 componentes)
│       │   ├── ChatInput.tsx      # Textarea principal con auto-resize
│       │   ├── ModelPicker.tsx    # Selector de modelo desde proveedores configurados
│       │   ├── AttachMenu.tsx     # Menú adjuntos (file, web search, reasoning, connectors, mode)
│       │   ├── ConnectorsSubmenu.tsx # Submenú de servidores MCP
│       │   └── ModeSwitch.tsx     # Toggle Chat/Agente
│       │
│       ├── providers\             # PROVEEDORES DE MODELO (3 componentes)
│       │   ├── ProviderCard.tsx
│       │   ├── ChooseProviderDialog.tsx
│       │   └── ConfigureProviderDialog.tsx
│       │
│       ├── settings\              # CONFIGURACIÓN (11 componentes)
│       │   ├── SettingsDialog.tsx # Modal 900×640, 8 tabs
│       │   ├── ThemePicker.tsx    # Grid selector de 13 temas
│       │   └── tabs\              # 8 tabs + primitives
│       │       ├── primitives.tsx
│       │       ├── GeneralTab.tsx
│       │       ├── AppearanceTab.tsx
│       │       ├── KeybindsTab.tsx
│       │       ├── ModelsTab.tsx
│       │       ├── McpTab.tsx
│       │       ├── MemoryTab.tsx
│       │       ├── SkillsTab.tsx
│       │       └── AgentsTab.tsx
│       │
│       ├── views\                 # VISTAS COMPLETAS (5 componentes)
│       │   ├── SessionsView.tsx
│       │   ├── SkillsView.tsx     # Tabs: Mis Skills / Explorar / Crear
│       │   ├── McpView.tsx        # Tabs: Conectados / Marketplace
│       │   ├── ChannelsView.tsx
│       │   └── MemoryView.tsx
│       │
│       ├── skills\                # SKILLS (5 componentes)
│       │   ├── SkillCard.tsx
│       │   ├── SkillCreator.tsx
│       │   ├── SkillDialog.tsx
│       │   ├── SkillExplorer.tsx
│       │   └── SkillInstaller.ts
│       │
│       ├── channels\              # CHANNELS (6 componentes)
│       │   ├── ChannelSidebar.tsx
│       │   ├── ChannelDialog.tsx
│       │   ├── InternalChannelView.tsx
│       │   ├── IntegrationStatusBadge.tsx
│       │   ├── TelegramIntegrationPanel.tsx
│       │   └── ComingSoonPanel.tsx
│       │
│       ├── memory\                # MEMORY (6 componentes)
│       │   ├── MemoryDialog.tsx
│       │   ├── MemoryEntryItem.tsx
│       │   ├── MemoryGraph.tsx
│       │   ├── MemoryGraphControls.tsx
│       │   ├── MemoryNodePanel.tsx
│       │   └── MemoryListView.tsx
│       │
│       ├── projects\              # PROJECTS
│       │   └── ProjectDialog.tsx
│       │
│       ├── mcp\                   # MCP (3 componentes)
│       │   ├── AddMcpServerDialog.tsx
│       │   ├── McpServerCard.tsx
│       │   └── McpToolItem.tsx
│       │
│       └── ui\                    # PRIMITIVOS shadcn/ui + BrandIcon (16 archivos)
│           ├── BrandIcon.tsx
│           ├── button.tsx
│           ├── card.tsx
│           ├── confirm-delete-dialog.tsx
│           ├── dialog.tsx
│           ├── dropdown-menu.tsx
│           ├── input.tsx
│           ├── scroll-area.tsx
│           ├── separator.tsx
│           ├── sheet.tsx
│           ├── sidebar.tsx        # Componente sidebar completo de shadcn (733 líneas)
│           ├── skeleton.tsx
│           ├── textarea.tsx
│           ├── tooltip.tsx
│           └── combobox.tsx           # Combobox con búsqueda (Combobox, Input, Content, List, Item, Empty)
│
├── scripts\                       # SCRIPTS DE DESARROLLO
│   └── seed-dev-data.ts           # Seed data para desarrollo (npm run seed)
│
├── dist\                          # BUILD OUTPUT (Vite)
└── dist-electron\                 # BUILD OUTPUT (Electron)

================================================================================
 4. ARQUITECTURA
================================================================================

 4.1 Flujo de la Aplicación:

   index.html
     → src/main.tsx (React root)
       → AppShell.tsx (shell raíz)
         → TitleBar.tsx (drag region, AppMenu, tabs, settings)
         → SidebarProvider → AppSidebar.tsx + Panel Activo
         → StatusBar.tsx
         → SettingsDialog.tsx (renderizado condicional)

   Panel Activo según mainView.type:
     - chat     → ChatArea.tsx
     - editor   → EditorPanel (placeholder)
     - terminal → TerminalPanel (placeholder)
     - agents   → AgentsPanel (placeholder)
     - sessions → SessionsView.tsx
     - skills   → SkillsView.tsx
     - mcp      → McpView.tsx
     - channels → ChannelsView.tsx
     - memory   → MemoryView.tsx

 4.2 Flujo de Estado:

   Zustand Stores ←(subscribe)→ React Components
        ↓
   types/ (interfaces compartidas)
        ↓
   persist middleware → localStorage (9 stores)

 4.3 Flujo de Datos:

   • Zustand stores son la única fuente de verdad.
   • 9 stores usan persist middleware → localStorage:
       sparta-chat, sparta-channels, sparta-memory, sparta-projects,
       sparta-providers, sparta-settings, sparta-skills, sparta-theme, sparta-ui.
   • 3 stores son en memoria: agent, event-bus, mcp.
   • Los hooks son wrappers delgados alrededor de los stores.
   • Event bus pub/sub con 36 tipos de eventos SpartaEvent:
       agent:started, agent:completed, agent:error
       thinking:started, thinking:token, thinking:completed
       tool:called, tool:result, tool:error
       mcp:connected, mcp:tool_discovered, mcp:disconnected
       stream:token, stream:completed, stream:error
       pipeline:step
       chat:message, message:deleted, message:edited, message:shared
       session:created, session:switched
       project:created, project:switched, project:deleted
       skill:created, skill:updated, skill:deleted, skill:invoked
       channel:created, channel:message, channel:deleted
       memory:added, memory:updated, memory:deleted

 4.4 Integración Electron:

   • vite-plugin-electron/simple compila:
       electron/main.ts → dist-electron/main.js
       electron/preload.ts → dist-electron/preload.mjs
   • En dev: carga desde Vite dev server URL
   • En prod: carga dist/index.html
   • Ventana: 1280×800 (min 900×600), titleBarStyle: 'hidden'
   • titleBarOverlay dinámico por tema (colores en theme.store.ts)
    • contextIsolation: true, nodeIntegration: false
    • IPC handlers: win:minimize, win:maximize, win:close, win:isMaximized,
                    titlebar:set-overlay, app:getVersion,
                    chat:send (streaming multi-proveedor: OpenAI/Anthropic/Ollama/local),
                    chat:abort,
                    memory:list, memory:addEntry, memory:updateEntry,
                    memory:deleteEntry, memory:addRelation, memory:updateRelation,
                    memory:removeRelation
    • Exposición via contextBridge:
        window.electronAPI → min/max/close/isMaximized/onMaximizedChange/
                              setTitleBarOverlay/getVersion
        window.electron → IPC genérico (on/send/invoke)
        window.sparta → eventos (onEvent/sendEvent) + sendMessage/abortMessage
                        sendMessage incluye apiUrl + isLocal para soporte multi-proveedor

 4.5 Routing:

   No hay router. La navegación es por estado de MainView (ui.store.ts):
     MainView =
       | { type: 'chat'; sessionId?: string }
       | { type: 'editor' }
       | { type: 'terminal' }
       | { type: 'agents' }
       | { type: 'sessions' }
       | { type: 'skills' }
       | { type: 'mcp' }
       | { type: 'channels'; channelId?: string }
       | { type: 'memory' }

   ui.store.ts también guarda previousMainView y provee goBack().

================================================================================
 5. COMPONENTES — DESCRIPCIÓN DETALLADA
================================================================================

 5.1 Layout

   AppShell.tsx
     └── Ubicación: src/components/layout/AppShell.tsx
     └── Propósito: Shell raíz de la aplicación
     └── Renderiza: TitleBar + SidebarProvider(AppSidebar + resize handle + panel) + StatusBar
     └── Lógica: initTheme() al montar, panel según mainView.type, animación viewFadeIn

   TitleBar.tsx
     └── Ubicación: src/components/layout/TitleBar.tsx
     └── Propósito: Titlebar personalizado con drag region
     └── Subcomponentes: botón toggle sidebar, AppMenu, logo+SpartaIcon, tabs (Chat/Editor/Terminal/Agents), settings
     └── Tabs principales: Chat, Editor, Terminal, Agents

   AppMenu.tsx
     └── Ubicación: src/components/layout/AppMenu.tsx
     └── Propósito: Menú dropdown tipo aplicación (File/Edit/View/Window/Help)
     └── Visual/placeholder: items renderizados sin handlers reales

   StatusBar.tsx
      └── Ubicación: src/components/layout/StatusBar.tsx
      └── Propósito: Barra de estado inferior
      └── Muestra: Gateway ready (verde), Agents, Cron, modelo activo (con BrandIcon), tokens,
                    versión desde package.json vía IPC (app:getVersion)

 5.2 Sidebar

   AppSidebar.tsx
     └── Ubicación: src/components/sidebar/AppSidebar.tsx
     └── Propósito: Sidebar principal completo basado en shadcn/ui/sidebar
     └── Header: logo SPARTA AGENT + botón "Nueva sesión" (⌘N)
     └── Navegación: 5 items con contadores (Sesiones, Skills, MCP, Canales, Memoria)
     └── Secciones: Fijados, búsqueda funcional de sesiones, Recientes
     └── Footer: botón Configuración
     └── Integrado en SidebarProvider; ancho controlado por ui.store.ts

   SidebarResizeHandle.tsx
     └── Ubicación: src/components/sidebar/SidebarResizeHandle.tsx
     └── Propósito: Redimensionar sidebar arrastrando el borde derecho
     └── Límites: 220px mínimo, 480px máximo

   SessionItem.tsx
     └── Ubicación: src/components/sidebar/SessionItem.tsx
     └── Propósito: Item de sesión con click para activar, hover actions
     └── Menú contextual: Fijar/Quitar fijado, Renombrar, Compartir, Eliminar
     └── Muestra tiempo relativo (ahora, Xm, Xh, Xd, Xmes)
     └── Diálogos: ConfirmDeleteDialog, share con sparta://session/{id}

   SessionList.tsx / ProjectSwitcher.tsx / ChannelItem.tsx / McpServerItem.tsx / SkillItem.tsx
     └── Componentes visuales legacy o simples; AppSidebar usa SessionItem directamente.

 5.3 Chat

   ChatArea.tsx
     └── Ubicación: src/components/chat/ChatArea.tsx
     └── Propósito: Orquesta la vista del panel de chat
     └── Si hay mensajes → MessageList + ChatInput; si no → HeroScreen + ChatInput

   HeroScreen.tsx
     └── Ubicación: src/components/chat/HeroScreen.tsx
     └── Propósito: Pantalla de bienvenida cuando no hay mensajes
     └── Animaciones Framer Motion (flotación, escala)
     └── Acciones rápidas: New chat, Coding, Research, Settings
     └── Texto descriptivo: "Elegiré las herramientas, explicaré el plan..."

   MessageBubble.tsx
      └── Ubicación: src/components/chat/MessageBubble.tsx
      └── Propósito: Renderiza un mensaje individual
      └── User: alineado derecha; Assistant: alineado izquierda con avatar SpartaIcon
      └── Soporta reasoningText → ThinkingBlock (AnimatePresence + auto-colapso),
          toolCalls → PipelineTrace
      └── ThinkingPill mientras isStreaming && !content && !reasoningText
      └── Barra de acciones inline SOLO ICONOS (sin texto) al hover vía CSS
      └── Actions por tipo:
          - Assistant: solo Copiar
          - User último mensaje: Copiar, Editar (inline), Reenviar (↻), Eliminar
          - User antiguos: solo Copiar
      └── Edición inline: textarea con botones Guardar/Cancelar (sin dialog)
      └── Eliminación directa: sin confirmación (borra al instante)
      └── Reenviar: reenvía el mismo texto al LLM vía sendMessage()

   MessageList.tsx
      └── Ubicación: src/components/chat/MessageList.tsx
      └── Propósito: Lista scrollable de mensajes
      └── Auto-scroll al último mensaje

   MessageActionsMenu.tsx / MessageActionsDialog.tsx
     └── Menú contextual con Share, Edit, Regenerate, Delete
     └── Delete usa ConfirmDeleteDialog
     └── Share genera sparta://session/{id}

 5.4 Reasoning

   ThinkingBlock.tsx
     └── Sección colapsable de razonamiento interno
   ThinkingPill.tsx
     └── Indicador animado "Thinking..." con 3 dots
   PipelineTrace.tsx
     └── Timeline visual de ejecución de herramientas
     └── Estados: done, error, running

 5.5 Input

   ChatInput.tsx
      └── Ubicación: src/components/input/ChatInput.tsx
      └── Propósito: Área de composición de mensajes
      └── Textarea auto-redimensionable (max 120px)
      └── Enter envía, Shift+Enter nueva línea
      └── Botones: Attach (+), ModelPicker (Combobox con búsqueda), Mic, Send (o Stop si isStreaming)
      └── Banner "No hay proveedores con API key" si ningún proveedor tiene key configurada
      └── ModelPicker usa Combobox con búsqueda: muestra modelo activo, al abrir permite buscar/filtrar
      └── Botón Stop (Square) reemplaza al de enviar durante streaming
      └── Envía mensaje vía useChatSession().sendMessage() que llama al provider por IPC
      └── Wrapper con sombra y borde de foco animado

   ModelPicker.tsx
     └── Ubicación: src/components/input/ModelPicker.tsx
     └── Propósito: Selector de modelo desde proveedores configurados
     └── Si no hay proveedores: botón "Configura un modelo" que abre settings
     └── Muestra BrandIcon del proveedor activo y lista modelos defaultModel

   AttachMenu.tsx
     └── Ubicación: src/components/input/AttachMenu.tsx
     └── Opciones: Agregar archivo (file input placeholder), Búsqueda web (toggle),
                    Razonamiento (toggle), Conectores (submenú MCP), ModeSwitch
     └── Click-outside para cerrar

   ConnectorsSubmenu.tsx
     └── Lista servidores MCP con checkbox para activar/desactivar

   ModeSwitch.tsx
     └── Toggle Chat / Agente en el modo de sesión

 5.6 Providers

   ProviderCard.tsx
     └── Card de proveedor registrado con BrandIcon, label, API key enmascarada,
         modelo default, fecha, acciones editar/eliminar

   ChooseProviderDialog.tsx
     └── Diálogo de selección de proveedor
     └── 13 cloud + 4 local = 17 vendors con BrandIcon

   ConfigureProviderDialog.tsx
     └── Configuración: nombre, API key (cloud) o Server URL (local)
     └── Botón "Test" llama fetchModelsByVendor() a la API real
     └── Select de modelos poblado tras test exitoso

 5.7 Settings

   SettingsDialog.tsx
     └── Modal 900×640 con sidebar de 8 tabs fijo
     └── Tabs: General, Appearance, Keybinds, Models, MCP, Memory, Skills, Agents
      └── Nota: SettingsTab unificado via interfaces/settings.interface (8 tabs).
          ui.store.ts y SettingsDialog.tsx importan el mismo type.

   ThemePicker.tsx
     └── Selector visual de 13 temas agrupados: Oscuros (8) + Claros (5)
     └── Oscuros: Midnight, Obsidian, Emerald, Rose, Amber, Dracula, Catppuccin, Nord
     └── Claros: Light, Lino, Salvia, Bruma, Arena

   Settings Tabs:
     └── GeneralTab     → Modelo default, memoria, idioma ES/EN, web search, reasoning
     └── AppearanceTab  → ThemePicker, tipografía, opciones ventana
     └── KeybindsTab    → Referencia de atajos
     └── ModelsTab      → Proveedores registrados + flujo 2-pasos
     └── McpTab         → Gestión MCP
     └── MemoryTab      → Estadísticas y toggle memoria
     └── SkillsTab      → CRUD de skills
     └── AgentsTab      → Lista y selección de agentes

 5.8 Views

   SessionsView.tsx
     └── Vista completa de gestión de sesiones (placeholder funcional simple)

   SkillsView.tsx
     └── Vista completa con 3 tabs: Mis Skills / Explorar / Crear
     └── Mis Skills: búsqueda, filtros por tags, grid de SkillCard, exportar a .skill.json
     └── Explorar: SkillExplorer carga skills desde /skills/manifest.json
     └── Crear: SkillCreator con preview y emoji picker

   McpView.tsx
     └── Vista completa con 2 tabs: Conectados / Marketplace
     └── Marketplace: 6 servidores preconfigurados (Filesystem, Git, SQLite, PostgreSQL, Puppeteer, Brave Search)
     └── Botón "Instalar" abre AddMcpServerDialog con config precargada

   ChannelsView.tsx
     └── Vista completa con sidebar interna (ChannelSidebar) + panel de contenido
     └── Canales internos → InternalChannelView (chat plano placeholder)
     └── Telegram → TelegramIntegrationPanel (test real de token)
     └── Discord/Slack/WhatsApp/Email → ComingSoonPanel

   MemoryView.tsx
      └── Vista completa con toggle Vista gráfico / Vista lista
      └── Grafo 3D Three.js: nodos esféricos (Fibonacci), aristas por categoría/proyecto
      └── OrbitControls manual, raycaster, panel flotante de nodo con info de sesión
      └── Empty state: "Aún no hay recuerdos" cuando entries.length === 0
      └── MemoryListView: CRUD de recuerdos
      └── Extracción automática al finalizar cada turno de chat (entidades/hechos/relaciones)

 5.9 Skills

   SkillCard.tsx       → Card con menú, toggle activar, exportar
   SkillCreator.tsx    → Formulario crear con preview y emoji picker
   SkillExplorer.tsx   → Explorador de skills descargables con filtros
   SkillInstaller.ts   → fetch manifest + install de skills
   SkillDialog.tsx     → Diálogo crear/editar skill (nombre, desc, prompt, tags)

 5.10 Channels

   ChannelSidebar.tsx          → Lista canales internos + integraciones
   ChannelDialog.tsx           → Crear canal con selector de icono SVG
   InternalChannelView.tsx     → Chat plano placeholder
   TelegramIntegrationPanel.tsx→ Config token/webhook + test real a api.telegram.org
   IntegrationStatusBadge.tsx  → Badge de estado con pulse
   ComingSoonPanel.tsx         → Placeholder para integraciones futuras

 5.11 Memory

   MemoryDialog.tsx        → Diálogo completo de gestión de memoria
   MemoryEntryItem.tsx     → Visualización de entrada individual
   MemoryGraph.tsx         → Grafo 3D Three.js
   MemoryGraphControls.tsx → Zoom/reset/toggle vista
   MemoryNodePanel.tsx     → Panel flotante con detalle del nodo
   MemoryListView.tsx      → Vista lista con CRUD

 5.12 MCP

   AddMcpServerDialog.tsx  → Agregar/editar servidor MCP (stdio/http)
   McpServerCard.tsx       → Card con toggle, tools, editar/eliminar
   McpToolItem.tsx         → Item individual de herramienta MCP

 5.13 Projects

   ProjectDialog.tsx       → Diálogo crear proyecto (nombre, descripción)

 5.14 UI Primitives (shadcn/ui)

   BrandIcon.tsx              → Icono de marca con detección light/dark + mono inversion
   button.tsx                 → 6 variantes, múltiples tamaños
   card.tsx                   → Card con Header/Title/Description/Content/Footer
   confirm-delete-dialog.tsx  → Diálogo reutilizable de confirmación
   dialog.tsx                 → Modal con overlay/portal/animations
   dropdown-menu.tsx          → Menú completo con items/grupos/checkboxes/submenús
   input.tsx                  → Input estilizado
   scroll-area.tsx            → Scrollbar custom
   separator.tsx              → Divisor horizontal/vertical
   sheet.tsx                  → Sheet/side panel
   sidebar.tsx                → Sidebar completo de shadcn/ui (733 líneas)
   skeleton.tsx               → Skeleton loader
   textarea.tsx               → Textarea estilizado
   tooltip.tsx                → Tooltip con posicionamiento

================================================================================
 6. SISTEMA DE ESTADOS (Zustand Stores)
================================================================================

   agent.store.ts
      └── Persist: No (en memoria)
      └── Estado: agents[] (vacío), activeAgentId, tasks{}
      └── Acciones: registerAgent, setActiveAgent, updateAgentStatus,
                    addTask, updateTask

   chat.store.ts
      └── Persist: Sí (sparta-chat)
      └── Estado: sessions[], activeSessionId, messagesBySession{}, isStreaming,
                  abortController
      └── Acciones: createSession (acepta model + providerId), switchSession,
                    deleteSession, pinSession, archiveSession, renameSession,
                    addMessage, updateMessage, deleteMessage,
                    appendContent, appendThinking, addToolCall, updateToolCallStatus,
                    setStreaming, startStreaming, stopStreaming (aborta con AbortSignal),
                    cleanupStaleSessions, getActiveMessages
      └── La primera pregunta del usuario renombra automáticamente la sesión
      └── cleanupStaleSessions() se llama al boot en AppShell

   channel.store.ts
      └── Persist: Sí (sparta-channels)
      └── Estado: channels[] (7 default: general, agentes + 5 integraciones como catálogo)
     └── Acciones: addChannel, deleteChannel, markRead, setActiveChannel,
                   updateIntegration

   event-bus.store.ts
     └── Persist: No (en memoria)
     └── Estado: events[] (últimos 500), listeners (Set)
     └── Acciones: dispatch, subscribe, clear

   mcp.store.ts
      └── Persist: No (en memoria)
      └── Estado: servers[] (vacío — antes tenía 3 default: Filesystem, Git, SQLite)
      └── Acciones: addServer, removeServer, setConnected, addTool,
                    removeTool, toggleServer

   memory.store.ts
      └── Persist: Sí (sparta-memory)
      └── Estado: entries[] (vacío), relations[], graphNodes[]
      └── Acciones: addEntry, updateEntry, deleteEntry,
                    addRelation, updateRelation, removeRelation,
                    rebuildGraph, addEntriesFromExtraction, getActiveCount

   project.store.ts
      └── Persist: Sí (sparta-projects)
      └── Estado: projects[] (1 default: sparta-agent con isTemplate: true), activeProjectId
      └── Acciones: addProject, switchProject, deleteProject, getActiveProject

   provider.store.ts
     └── Persist: Sí (sparta-providers)
     └── Estado: providers[]
     └── Acciones: addProvider, updateProvider, removeProvider, getByVendor
     └── Helper: getVendorLabel(vendor) → string

   settings.store.ts
      └── Persist: Sí (sparta-settings)
      └── Estado: settingsOpen, defaultModel, input, activeModel,
                  memoryEnabled, webSearchEnabled, reasoningEnabled,
                  sessionMode ('chat'|'agent'), apiKeys{}, language ('es'|'en')
      └── Acciones: openSettings, closeSettings, setInput, setDefaultModel,
                    setApiKey, toggleMemory, toggleWebSearch,
                    toggleReasoning, setSessionMode, setLanguage
      └── sendMessage() eliminado — vive en useChatSession hook + chat IPC

   skill.store.ts
      └── Persist: Sí (sparta-skills)
      └── Estado: skills[] (2 default con isTemplate: true), activeSkillIds[]
     └── Acciones: addSkill, updateSkill, deleteSkill, toggleActive, isActive

   theme.store.ts
     └── Persist: Sí (sparta-theme)
     └── Estado: theme
     └── Acciones: setTheme (aplica DOM data-theme + data-theme-mode + overlay)
     └── Helper: initTheme() — lee localStorage y aplica tema al boot
     └── Temas: midnight (default), obsidian, emerald, rose, amber,
                 dracula, catppuccin, nord, light, linen, sage, mist, sand

   ui.store.ts
      └── Persist: Sí parcial (sparta-ui)
      └── Estado: sidebarOpen, sidebarWidth (280 default), contextPanelOpen,
                  mainView, previousMainView, sidebarExpandedSections{},
                  activeSettingsTab (8 valores, importa SettingsTab de interfaces)
      └── Acciones: toggleSidebar, setSidebarWidth (220-480), toggleContextPanel,
                    setMainView, goBack, toggleSidebarSection, setActiveSettingsTab

================================================================================
 7. SISTEMA DE TIPOS
================================================================================

    9 módulos en src/types/ re-exportan desde src/interfaces/ (backward compat).
    Las interfaces verdaderas viven en src/interfaces/:

    Agent (agent.interface.ts)
      ── AgentType: 'coding' | 'research' | 'automation' | 'project' | 'mcp'
      ── AgentStatus: 'idle' | 'running' | 'thinking' | 'error' | 'completed'
      ── Agent: { id, name, type, status, model, createdAt, tools[], description }
      ── Task: { id, agentId, description, status, steps[], createdAt, completedAt? }
      ── TaskStep: { id, name, status, tool?, durationMs?, error? }

    Chat (chat.interface.ts)
      ── MessageRole: 'user' | 'assistant' | 'system'
      ── Message: { id, role, content, timestamp, sessionId, agentId?,
                    thinking?, reasoningText?, isStreaming?, toolCalls?, pipelineSteps? }
      ── ToolCall: { id, toolName, input, output?, status, durationMs?, error? }
      ── PipelineStep: { name, status, timestamp }
      ── Session: { id, title, createdAt, updatedAt, model, providerId?,
                    messageCount, agentId?, pinned?, archived? }
      ── ChatRequest: { model, messages, system?, stream?, maxTokens?, temperature? }
      ── ChatStreamChunk: { type, delta?, toolCall?, error? }

   types/events.ts
     ── 36 eventos tipo unión SpartaEvent:
         agent:started / agent:completed / agent:error
         thinking:started / thinking:token / thinking:completed
         tool:called / tool:result / tool:error
         mcp:connected / mcp:tool_discovered / mcp:disconnected
         stream:token / stream:completed / stream:error
         pipeline:step
         chat:message / message:deleted / message:edited / message:shared
         session:created / session:switched
         project:created / project:switched / project:deleted
         skill:created / skill:updated / skill:deleted / skill:invoked
         channel:created / channel:message / channel:deleted
         memory:added / memory:updated / memory:deleted

   types/mcp.ts
     ── MCPServerType: 'stdio' | 'http'
     ── MCPServerConfig: { id, name, type, command?, args?, url?, enabled }
     ── MCPServer: { id, name, type, connected, tools[], config }
     ── MCPTool: { name, description, inputSchema, serverId }

   types/project.ts
     ── Project: { id, name, icon?, color?, createdAt, description? }

   types/skill.ts
     ── SkillCategory: 'Coding' | 'Research' | 'Writing' | 'Analysis' | 'Automation'
     ── Skill: { id, name, description, prompt, icon?, tags[], createdAt }
     ── DownloadableSkill: { id, name, description, version, author, icon,
                              tags, category, prompt, featured, installed? }

   types/channel.ts
     ── ChannelKind: 'internal' | 'integration'
     ── IntegrationProvider: 'telegram' | 'discord' | 'slack' | 'whatsapp' | 'email'
     ── IntegrationStatus: 'not_configured' | 'connecting' | 'connected' | 'error'
     ── IntegrationConfig: { provider, status, botToken?, webhookUrl?,
                              workspaceToken?, errorMessage?, botInfo? }
     ── Channel: { id, name, topic?, members?, createdAt, unreadCount, kind,
                   integration?, icon? }
     ── ChannelMessage: { id, channelId, authorId, content, timestamp }

   Memory (memory.interface.ts)
      ── MemoryEntry: { id, content, source: 'manual'|'auto', category?,
                         createdAt, projectId?, sourceSessionId?, sourceMessageId? }
      ── MemoryRelation: { fromId, toId, type, weight, entityType? }
      ── RelationType: 'same_category' | 'same_project' | 'same_session' |
                        'manual' | 'entity_relation'
      ── MemoryGraphNode: { memoryId, position: {x,y,z}, radius, color }
      ── MemoryGraph: { nodes: MemoryGraphNode[], relations: MemoryRelation[] }
      ── ExtractedMemory: { entities: [], facts: [], relations: [] }

   Provider (provider.interface.ts)
      ── ProviderVendor: 17 valores
          cloud: anthropic | openai | google | groq | mistral | azure |
                 deepseek | together | fireworks | openrouter | cohere |
                 perplexity | xai
          local: ollama | lmstudio | llamacpp | custom
      ── ProviderKind: 'cloud' | 'local'
      ── Provider: { id, vendor, kind, label, apiKey?, serverUrl?,
                     defaultModel?, createdAt, usage? }
      ── ModelInfo: { id, name, vendor, providerId }
      ── AIProvider: { vendor, kind, listModels, testConnection, streamChat }
      ── ConnectionTestResult: { ok, error? }

================================================================================
 8. SISTEMA DE TEMAS (Design Tokens)
================================================================================

   13 Temas: 8 oscuros + 5 claros
     Oscuros: midnight (default), obsidian, emerald, rose, amber,
              dracula, catppuccin, nord
     Claros:  light, linen, sage, mist, sand

   Aplicado via atributo data-theme en <html>. Cada tema sobreescribe variables
   CSS en :root (definidas en globals.css + archivos en src/styles/themes/).

   Atributo data-theme-mode: "dark" para temas oscuros, "light" para claros.
   Usado por .brand-icon-mono para invertir logos monocromáticos
   (Anthropic, OpenAI) en modos oscuros via filter: invert(1).

   DARK_THEMES exportado desde theme.store.ts; useTheme() expone isDark.

   Variables CSS principales:

     Backgrounds:
       --bg-base, --bg-sidebar, --bg-surface, --bg-elevated, --bg-hover,
       --bg-active, --bg-input, --bg-modal

     Texto:
       --text-display, --text-primary, --text-secondary, --text-muted

     Acento:
       --accent, --accent-dim, --accent-muted, --accent-glow

     Bordes:
       --border-subtle, --border-normal, --border-strong, --border-focus

     Estados:
       --status-ok, --status-warn, --status-err, --status-think

     Radios:
       --radius-sm: 3px, --radius-md: 6px, --radius-lg: 10px, --radius-xl: 14px

     Fuentes:
       --font-ui: Inter
       --font-display: Space Grotesk
       --font-mono: Geist Variable

     Dimensiones:
       --titlebar-h: 38px
       --sidebar-w: variable (220-480px)
       --statusbar-h: 24px

================================================================================
 9. DEPENDENCIAS PRINCIPALES
================================================================================

   Producción (23):
     react ^18.2.0, react-dom ^18.2.0
     @base-ui/react ^1.6.0
     @fontsource-variable/geist ^5.2.9
     @fontsource/inter ^5.2.8
     @fontsource/space-grotesk ^5.2.10
     @monaco-editor/react ^4.7.0
     @tailwindcss/vite ^4.3.1
     @tanstack/react-query ^5.101.1
     @xterm/xterm ^6.0.0, @xterm/addon-fit ^0.11.0
     class-variance-authority ^0.7.1
     clsx ^2.1.1
     framer-motion ^12.41.0
     lucide-react ^1.21.0
     monaco-editor ^0.55.1
     shadcn ^4.11.0
     tailwind-merge ^3.6.0
     tailwindcss ^4.3.1
     three 0.184.0
     tw-animate-css ^1.4.0
     zustand ^5.0.14

   Desarrollo (15):
     electron ^30.0.1
     electron-builder ^24.13.3
     vite ^5.1.6
     vite-plugin-electron ^0.28.6
     vite-plugin-electron-renderer ^0.14.5
     @vitejs/plugin-react ^4.2.1
     typescript ^5.2.2
     eslint ^8.57.0
     @typescript-eslint/eslint-plugin ^7.1.1
     @typescript-eslint/parser ^7.1.1
     eslint-plugin-react-hooks ^4.6.0
     eslint-plugin-react-refresh ^0.4.5
     @types/react ^18.2.64
     @types/react-dom ^18.2.21
     @types/three ^0.184.1

================================================================================
 10. ELECTRON-BUILDER CONFIG
================================================================================

   Configuración principal en package.json (también reflejada parcialmente en
   electron-builder.json5 si aplica):

   appId: com.sparta.agent
   productName: Sparta Agent
   directories.output: release
   files: ["dist", "dist-electron"]
   main: dist-electron/main.js
   icon: public/sparta-escritorio.png

   macOS: DMG
     → Sparta-Agent-${version}.dmg

   Windows: NSIS x64
     → Sparta-Agent-${version}-Setup.exe
     → oneClick: false, allowToChangeInstallationDirectory: true

   Linux: AppImage
     → Sparta-Agent-${version}.AppImage

================================================================================
 11. COMANDOS DE DESARROLLO
================================================================================

   pnpm dev      → Inicia Vite + Electron (dev server + electron)
   pnpm build    → tsc + vite build + electron-builder (package installer)
   pnpm lint     → ESLint (.ts, .tsx) con --max-warnings 0
   pnpm preview  → Vite preview del build

================================================================================
 12. OBSERVACIONES IMPORTANTES
================================================================================

   ✔ Etapa funcional de UI/UX (v0.8.0) — todos los sistemas de datos operativos
   ✔ Capa de IA funcional: transports Anthropic/OpenAI/Ollama + factory + gateway
   ✔ Chat real con streaming token-a-token vía IPC (main process, multi-proveedor)
   ✔ ThinkingBlock/ThinkingPill/PipelineTrace con datos reales del proveedor
   ✔ Abortar generación con AbortSignal + botón Stop en ChatInput
   ✔ Extracción automática de memoria como grafo de conocimiento
   ✔ Combobox con búsqueda en ModelPicker y ConfigureProviderDialog
   ✔ Acciones inline solo iconos (CSS hover) con patrón Claude Code
   ✔ Edición inline sin dialog en mensajes
   ✔ Chat IPC multi-proveedor (OpenAI, Anthropic, Ollama, local)
   ✔ Sin router — navegación por estado MainView (9 vistas)
   ✔ 9 stores con persist middleware → localStorage
   ✔ Sistema i18n español/inglés con hook useTranslation()
   ✔ 13 temas visuales (8 oscuros + 5 claros) con ThemePicker agrupado
   ✔ Sistema de proveedores de modelo (17 vendors) con test connection real
   ✔ fetchModelsByVendor() — llama APIs reales para listar modelos
   ✔ Sidebar redimensionable (220-480px) con manejador propio
   ✔ Pin/archive/rename de sesiones funcional
   ✔ Búsqueda funcional de sesiones en sidebar
   ✔ Grafo 3D de memoria con Three.js interactivo + extracción automática
   ✔ Test real de token de Telegram contra api.telegram.org
   ✔ 12 skills descargables precargadas en public/skills/
   ✔ Marketplace MCP con 6 servidores preconfigurados
   ✔ Iconos de marca con detección automática light/dark
   ✔ titleBarStyle: 'hidden' + titleBarOverlay dinámico por tema
   ✔ contextIsolation: true, nodeIntegration: false
   ✔ Versión única desde package.json vía IPC (app:getVersion)
   ✔ SettingsTab unificada (8 tabs) desde interfaces, sin duplicación local

================================================================================
 13. COSAS PENDIENTES / POR HACER
================================================================================

   [ ] Integrar Monaco Editor real en EditorPanel
   [ ] Integrar xterm.js real en TerminalPanel
   [ ] Implementar Agent Management UI real en AgentsPanel
   [ ] Implementar atajos de teclado funcionales (KeybindsTab)
   [ ] Implementar adjuntos reales (File, Code, Image, URL)
   [ ] Implementar grabación de audio (Mic)
   [ ] Migrar estilos inline a Tailwind completamente
   [ ] Configurar GitHub repo
   [ ] Escribir tests (prioridad: transports + extractor de memoria)
   [ ] Configurar CI/CD
   [ ] Implementar sistema de plugins/extensiones
   [ ] Conectar MCP servers reales (stdio/http) en lugar de mock actual
   [ ] Implementar integraciones Discord/Slack/WhatsApp/Email reales
   [ ] Implementar Skills invocation real durante el chat
   [ ] Implementar Projects multi-proyecto real con contexto
   [ ] Crear sparta-core/ (Rust) — vault de API keys + sandbox MCP
   [ ] Migrar persistencia de memoria de localStorage a SQLite vía IPC
   [ ] Mejorar el Combobox cuando el proveedor retorna muchos modelos (virtualización)

================================================================================
   FIN DEL DOCUMENTO
================================================================================
