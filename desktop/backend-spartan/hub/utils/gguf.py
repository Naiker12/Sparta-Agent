# Minimal hub utils gguf stub
from typing import Any, List, Optional
from pathlib import Path


def extract_quant_label(name: str | Path | None) -> Optional[str]:
    return None


def iter_snapshots_preferring_whole(*args, **kwargs) -> List[Any]:
    return []
