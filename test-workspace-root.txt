TEST: Verificación de workspace root
=====================================

Este archivo se creó con write_file_tool.
Si ves esto, el workspace root se está resolviendo correctamente.

--- Pruebas manuales ---

1. write_file_tool: Ya funcionó (este archivo existe).

2. read_file_tool: Pide al agente que lea este archivo.
   → Debería mostrar este contenido sin errores.

3. patch_file_tool: Pide al agente que edite este archivo
   (ej: cambiar "manuales" por "automáticas").
   → Debería mostrar diff y aplicar el cambio.

4. delete_file_tool: Pide al agente que elimine este archivo.
   → Debería moverse a .sparta/.trash/ en vez de borrarse directamente.

5. Sin carpeta abierta: Cierra la carpeta, envía "lee test-workspace-root.txt".
   → Debe fallar con "SPARTA_WORKSPACE_ROOT no está definida".
   → No debe leer archivos del directorio de Sparta Agent.
