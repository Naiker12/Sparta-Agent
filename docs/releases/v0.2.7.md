# Sparta Agent v0.2.7

## Corrección de publicación

- Se regeneró `package-lock.json` para que coincida con `package.json`.
- `npm ci` vuelve a funcionar en los runners limpios de GitHub Actions.
- Incluye todas las mejoras de workspace y agente entregadas en v0.2.6.

## Verificación

- `npm ci --dry-run --ignore-scripts`, lint, typecheck, pruebas raíz, regresiones Electron y build completo superados.
