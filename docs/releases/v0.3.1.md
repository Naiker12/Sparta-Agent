# Sparta Agent v0.3.1

## Corrección

- Se corrige la validación posterior al empaquetado de Windows. Ya no depende de la variable de entorno `npm_package_version`, que las versiones actuales de npm no siempre exponen; la versión se obtiene de `package.json`.

## Validación

- Comprobación de tipos de TypeScript.
- Prueba automatizada de regresión del empaquetado.
